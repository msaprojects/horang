name: horang
description: Aplikasi Horang

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# version: 2.1.0+1
version: 2.0.5+10

environment:
  sdk: ">=2.7.0+1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  # cupertino_icons: any
  cupertino_icons: 1.0.2 #untuk mengambil beberapa style icon
  flushbar: 1.10.4 #untuk desain flushbar dipakai di log
  http: any #untuk koneksi http
  minimize_app: ^0.1.1 #untuk fungsi minimaze app 
  shared_preferences: ^0.5.12+4 #untuk fungsi sharepreference
  indonesia: 1.0.1 #untuk fungsi format rupiah 
  flutter_launcher_icons: 0.9.0 #untuk fungsi icon louncher
  local_auth: ^0.6.3+4 #untuk fungsi fingerprint
  google_fonts: ^1.1.2 #untuk ambil font dari googlefont
  flutter_swiper: 1.1.6 #untuk fungsi swiper di desain voucher
  email_validator: 2.0.1 #untuk fungsi format penulisan email
  timeline_tile: ^2.0.0 #untuk fungsi desain timeline eg : dipakai di file log
  connectivity: 3.0.3 #untuk fungsi pengecekan koneksi
  commons: ^0.7.8+3 #untuk alert notif 
  syncfusion_flutter_datepicker: ^19.2.46 #fungsi syncfusion tanggal di order list
  fluttertoast: 7.1.6 #untuk fungsi toast
  firebase_messaging: ^7.0.3 #untuk fungsi notifikasi yang diambil dari firebase
  time_picker_widget: ^1.0.0+9 #untuk fungsi mengambil timepicker
  intl_phone_number_input: any #untuk fungsi textfield dengan kode phone di berbagai negara e.g : dipakai di konfirmpayment
  grouped_list: 4.1.0 #untuk fungsi grouping berdasarkan suatu parameter e.g : dipakai di logaktivitas
  new_version: any #untuk pengecekan latest version di playstore / di appstore
  get: ^4.1.4 #untuk fungsi obx pada saat scrolling sk

  # pin_code_text_field: 1.8.0
  # http: ^0.12.1
  # shared_preferences: ^0.5.7
  # date_format: ^1.0.9
  # numeric_keyboard: ^1.1.0
  # pin_code_view: 0.2.1
  # flutter_native_timezone: ^2.0.0
  # imei_plugin: ^1.2.0
  # ffi: ^1.0.0
  # upgrader: ^2.8.1
  # in_app_update: ^2.0.0
  # get_version: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: true
  ios: true
  image_path: "assets/image/logogudang.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
  - assets/image/
  - assets/res/syaratketentuan.txt

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
